import React from "react"
import  {createRoot} from "react-dom/client"
// import FormVal from "./formVal/FormVal"
import Router from './formVal/Router';






createRoot(document.getElementById("root")).render(<Router/>)